# Terraform-linters Tap

`brew install terraform-linters/tap/tflint`

Or `brew tap terraform-linters/tap` and then `brew install tflint`.

Or, in a `brew bundle` `Brewfile`:

```ruby
cask "terraform-linters/tap/tflint"
```
